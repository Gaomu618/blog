# 学习自动化

把博客里的"重点"自动变成 Anki 卡片,按间隔重复算法复习。

> **核心思路**: 写博客时顺手标记 Q&A → 一行命令生成 Anki 包 → Anki 按算法每天提醒你复习

---

## 🚀 5 分钟上手

### 第 1 步: 装 Anki(只做一次)

1. 去 https://apps.ankiweb.net/ 下载(免费,跨平台)
2. Windows 用户选 `Standard installer` 装
3. 打开 Anki,创建一个 deck(牌组),比如叫 **Java 学习**

### 第 2 步: 写博客时标记卡片

在任意 `.md` 文件里,用这种语法标记一张卡片:

```markdown
<!-- FLASH_CARD
Q: Java 有哪 8 种基本数据类型?
A: byte / short / int / long / float / double / char / boolean
-->
```

可以写任意张。每张卡片用 `<!-- FLASH_CARD Q: ... A: ... -->` 包起来。

**写完保存就行**——HTML 注释不会在博客页面上显示,只会被脚本识别。

### 第 3 步: 一行命令生成卡片

新开 PowerShell:

```powershell
cd C:\Users\dlf\Desktop\my-blog
py generate_anki.py src\content\blog\*.md
```

会看到:
```
扫描 1 个文件:
  src\content\blog\hi-im-tagaki.md: 4 张
✅ 已生成 cards.csv (4 张卡片)
```

### 第 4 步: 导入 Anki

1. 打开 Anki
2. **File → Import**(或快捷键)
3. 选项目里的 `cards.csv`
4. 设置:
   - **Field separator**: Comma
   - **Fields**:
     - 第 1 个字段 → **Front**
     - 第 2 个字段 → **Back**
   - **Deck**: 选你刚建的 "Java 学习"
5. 点 **Import**

### 第 5 步: 每天复习

打开 Anki → 选 deck → 自动显示当天该复习的卡片。
- 答对 → 卡片下次出现时间变长(1 天 → 3 天 → 7 天 → 30 天)
- 答错 → 卡片很快再出现

**Anki 算法自己会安排节奏**,你只要坚持每天打开。

---

## 📋 卡片标记语法详解

### 基本卡片(Q + A)

```markdown
<!-- FLASH_CARD
Q: 问题
A: 答案
-->
```

### 代码类答案(用三引号不会冲突)

```markdown
<!-- FLASH_CARD
Q: Java 里如何遍历一个 HashMap?
A: 三种方式:
   1. entrySet()
   2. keySet() + get()
   3. forEach (Java 8+)
-->
```

### 提取整篇博客的卡片

```powershell
py generate_anki.py src\content\blog\*.md
```

通配符 `*.md` 匹配所有文件,每篇博客里标记的卡片都会被收集。

---

## 🛠️ 工具脚本说明

`generate_anki.py` 做了什么:

1. 用正则扫描 `<!-- FLASH_CARD ... Q: ... A: ... -->` 块
2. 解析 Q 和 A 两行
3. 输出 `cards.csv`(UTF-8 with BOM,Excel/Anki 都能直接读)
4. 中文不乱码

### 常用命令

| 命令 | 干啥 |
|---|---|
| `py generate_anki.py src\content\blog\hi-im-tagaki.md` | 单文件 |
| `py generate_anki.py src\content\blog\*.md` | 整批所有博客 |
| `py generate_anki.py *.md` | 当前目录所有 .md |
| `py generate_anki.py post1.md post2.md post3.md` | 多个指定文件 |

---

## 🔁 建议的日常工作流

```
学完一个 Java 知识点
   ↓
写一篇博客(像 hi-im-tagaki.md 那样)
   ↓
在博客里标记 3-5 张 FLASH_CARD
   ↓
git add + commit + push  ← 同步博客
   ↓
py generate_anki.py src\content\blog\*.md  ← 生成卡片
   ↓
Anki 导入 cards.csv
   ↓
每天打开 Anki 复习
```

**一天 5-10 分钟**就能把当天学的东西变成长期记忆。

---

## 🚀 进阶(以后再做)

### 1. AI 自动出题(不写 Q/A 也能生成)

写完博客,跑一个脚本让 AI 读完整篇内容,自动生成问答对。
- 需要 LLM API(OpenAI / Claude / 国内大模型)
- 我能帮你搭

### 2. 卡片 + 博客双向链接

每张卡片里加 "出处: 博客 URL"——复习时能点过去看完整文章。

### 3. 复习统计可视化

Anki 本身有统计。但可以加一个脚本把数据导出,生成"每周复习报告"。

### 4. 每天 21 点自动提醒复习

Windows 任务计划程序 + 弹窗提醒:
- "今天有 15 张卡片待复习"
- 点确定直接打开 Anki

---

## ⚠️ 几个常见问题

### Q1: 博客上会看到 `<!-- FLASH_CARD ... -->` 吗?
**不会**。`<!-- -->` 是 HTML 注释,Markdown 渲染时会忽略,博客页面看不到。
你只会在 .md 源文件里看到(用 VSCode 编辑时)。

### Q2: 一张卡片能在博客里重复出现吗?
**可以**。比如同一概念在不同博客里都问一次,复习时重复强化。

### Q3: 卡片要不要写完整答案?
**看你**。简答(Q+A 一句话)复习效果好,长答案适合"概念解释"。
建议: **简答优先**,长答案拆成多张卡。

### Q4: Anki 同步问题?
- **AnkiWeb 免费同步**: 装 Anki 时注册个账号,多设备自动同步
- 不想注册: 单机用也完全 OK

### Q5: 不想用 Anki 怎么办?
可以把 `cards.csv` 导入:
- **Quizlet**(在线平台,免费层够用)
- **MarginNote**(Mac/iPad)
- **Obsidian + Spaced Repetition 插件**(本地 Markdown)

---

## 📁 文件清单

```
my-blog/
├── generate_anki.py        # 这个脚本(本仓库根目录)
├── 学习自动化.md            # 本文档
├── cards.csv               # 每次生成的卡片(可加入 .gitignore)
└── src/content/blog/
    └── *.md                # 你的博客(里面带 FLASH_CARD 块)
```

> 💡 建议把 `cards.csv` 加到 `.gitignore`,因为它是生成的,不是源文件。
