# 推送流程

把"写完/改完文章"同步到 GitHub 的完整操作手册。看完能自己独立推送，不用再问。

---

## 🚀 日常 3 步（写新文章 / 改完文章后跑）

新开 PowerShell 窗口，**每条命令一行一回车**：

```powershell
cd C:\Users\dlf\Desktop\my-blog
git add -A
git commit -m "这里写你这次干了啥"
git push
```

跑完看到下面这行，就说明成功了：
```
To https://github.com/Gaomu618/blog.git
   xxxxxx..yyyyyy  main -> main
```

> 看到 `main -> main` = 快递已送达 ✅

---

## 📖 4 条命令分别是什么

| # | 命令 | 干啥 | 快递类比 |
|---|---|---|---|
| 1 | `cd C:\Users\dlf\Desktop\my-blog` | 走到 my-blog 文件夹 | 走到快递点 |
| 2 | `git add -A` | 把所有改动暂存 | 放进购物车 |
| 3 | `git commit -m "..."` | 打包 + 贴说明 | 装箱 + 写快递单 |
| 4 | `git push` | 推送到 GitHub | 交给顺丰小哥 |

> 简记：**走到 → 装车 → 贴单 → 寄出**

---

## ✍️ 三种常见场景

### 场景 1：写一篇新文章

```powershell
# 1. 在 VSCode 里新建并写完文件
code src\content\blog\my-new-post.md

# 2. 保存后,跑推送 3 步
cd C:\Users\dlf\Desktop\my-blog
git add -A
git commit -m "新文章: Java 学习路线笔记"
git push
```

### 场景 2：改已经发布的文章

```powershell
# 1. VSCode 打开
code src\content\blog\hi-im-tagaki.md

# 2. 改完保存(Ctrl+S),跑推送 3 步
cd C:\Users\dlf\Desktop\my-blog
git add -A
git commit -m "修改: 改了第一段自我介绍"
git push
```

### 场景 3：删除一篇文章

```powershell
cd C:\Users\dlf\Desktop\my-blog
git rm src\content\blog\xxx.md      # 删文件 + 标记
git commit -m "删除: xxx 文章"
git push
```

---

## 💡 写好 commit message（贴单的艺术）

✅ **人话 + 具体**：
- `"新文章: JDK/JRE/JVM 三者关系"`
- `"修改: 修了第一段的错别字"`
- `"删除: hello-world 示例文章"`
- `"调整: 改了 About 页的技术栈"`

❌ **太模糊**：
- `"update"`、`"fix"`、`"改了点东西"`、`"test"`

**规则**：你自己一周后看 commit 历史，能立刻想起"这版干了啥"，就对了。

---

## 🔍 调试用命令（不会推送，只是看状态）

| 命令 | 干啥 |
|---|---|
| `git status` | 看**现在有啥改动**没提交 |
| `git status -s` | 简短版（一行一个文件）|
| `git log --oneline` | 看**提交历史**（每行一条）|
| `git log --oneline -5` | 最近 5 条 |
| `git diff` | 看"还没 add"的具体改动 |
| `git diff --staged` | 看"已 add 但没 commit"的内容 |
| `git remote -v` | 看远程仓库地址（应该是 `github.com/Gaomu618/blog.git`）|

---

## 🚨 常见报错 + 解决方法

### 1. `fatal: not a git repository`
**原因**：没 cd 到 my-blog 目录。

```powershell
cd C:\Users\dlf\Desktop\my-blog   # ← 跑这行先
```

### 2. `Please tell me who you are`
**原因**：没配 git 用户名/邮箱。

```powershell
git config --global user.name "Tagaki"
git config --global user.email "2927132157@qq.com"
```

### 3. `failed to push some refs ... (non-fast-forward)`
**原因**：远程（GitHub）有你本地没有的新 commit（可能你在网页上直接改了东西）。

```powershell
git pull        # 先把远程的拉下来
# 解决冲突(如果有)后:
git push
```

### 4. 提交信息写错了（没 push 之前）
```powershell
git commit --amend -m "新的正确的提交信息"
```

### 5. 撤销**还没 commit** 的本地改动
```powershell
git checkout -- src\content\blog\xxx.md
```

### 6. 撤销**刚 commit**（保留文件改动）
```powershell
git reset --soft HEAD~1
```

### 7. 网络错误 / push 卡住
等 10 秒；还不行就：
```powershell
git push --retry                # 重新试
# 或重新检查网络
Test-NetConnection github.com -Port 443
```

---

## 🧠 进阶（什么时候再看）

- **同时改多篇文章怎么分别提交**：`git add 文件1; git commit -m "..."; git add 文件2; git commit -m "..."`
- **撤销已经 push 的 commit**：网页上点 `Revert`，或在本地 `git revert <commit-hash>` 然后 push
- **改文件名（不丢历史）**：`git mv 旧名.md 新名.md`，然后 commit + push
- **看某个文件的历史**：`git log --follow 文件路径`

---

## ✅ 第一次推送时,可以这样检查清单

1. [ ] 已经 `cd C:\Users\dlf\Desktop\my-blog`
2. [ ] 跑过 `git status` 看到有改动（D / A / ??）
3. [ ] 跑过 `git add -A`
4. [ ] `git commit -m "..."` 引号没漏
5. [ ] `git push` 看到 `main -> main`

5 条都满足,99% 成功。
